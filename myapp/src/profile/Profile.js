import React from 'react'

const Profile = (props) => {
    console.log(pros);
    return (
        <div>
        <p>{props.fullname}</p>
        <p>{props.bio}</p>
        <p>{props.profession}</p>

            
        </div>
    )
}

export default Profile
